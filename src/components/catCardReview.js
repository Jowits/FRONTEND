{
  /* <Comment.Group>
<Comment>
  <Header as="h3" dividing>
    Comments
  </Header>
  <Comment.Content>
    <Comment.Author>
      <h3>{this.props.cat.user.username}</h3>
    </Comment.Author>
    <Comment.Text>
      {this.props.cat.reviews.map(review => (
        <h4>{review.text}</h4>
      ))}
    </Comment.Text>
  </Comment.Content>
</Comment>
</Comment.Group> */
}
